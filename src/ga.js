/* import ReactGA from 'react-ga' */

// const accountIds = {
// development: 'UA-142800714-1'
// }

// ReactGA.initialize(accountIds[process.env.NODE_ENV] || accountIds.development, { debug: process.env.NODE_ENV !== 'production' })

// console.log(ReactGA)
